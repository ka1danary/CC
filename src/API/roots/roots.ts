export const BASE_URL = "http://localhost:3000";

const main_root = "/";
const user_root = "/USER";
const user_request = '/USER_REQUEST'
const session_root = "/SESSION";
const session_status_root = "/S_STATUS";
const user_balance_root = "/BALANCE";

export {
  main_root,
  user_root,
  session_root,
  session_status_root,
  user_balance_root,
  user_request
};
